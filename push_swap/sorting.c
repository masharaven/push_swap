#include <stdio.h>
char *merge(char *a)
{
	while (ft_strlen(a) > 1)
	
}

char	t_sorted(char *a)
{
	int	i;
	int	j;
	int	res[];

	while (i < ft_strlen(a) && j < ft_strlen())
}

int main(int argc, char **argv)
{
	int i = argc; // количество элементов
	//int a[] = {2,6,9,1,7,4};
	int	*a = argv;
	printf("%s", ft_sorted(a[i]));
	return 0;
}